services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: master-gambar:latest
    container_name: master-gambar-app
    working_dir: /var/www/html
    depends_on:
      mysql:
        condition: service_healthy
    env_file:
      - .env.docker
    volumes:
      - .:/var/www/html          # <-- Sinkronisasi code dari laptop ke container
      - app-code:/var/www/html   # <-- Berbagi volume dengan Nginx pada folder yang sama
    networks:
      - master-gambar-network

  nginx:
    image: nginx:1.27-alpine
    container_name: master-gambar-nginx
    depends_on:
      - app
    # Perintah tunggu disederhanakan untuk memeriksa folder public
    command: sh -c "until [ -d /var/www/html/public ]; do sleep 2; done; nginx -g 'daemon off;'"
    ports:
      - "8080:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - app-code:/var/www/html:ro  # <-- Sekarang akan membaca data dari target yang benar
    networks:
      - master-gambar-network

  mysql:
    image: mysql:9.7.0
    container_name: master-gambar-mysql
    environment:
      MYSQL_DATABASE: db_master
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: app
      MYSQL_PASSWORD: secret
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - master-gambar-network

volumes:
  mysql-data:
  app-code:

networks:
  master-gambar-network:
    driver: bridge